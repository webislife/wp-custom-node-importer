# WordPress Jewels Importer

> Актуальный пример файла каталога `testCatalog.csv`

## Доступные команды

- `git pull origin master` обновить
- `npm install` обновление зависимостей
- `CATALOG=testCatalog.csv node import.js` запуск  импорта
